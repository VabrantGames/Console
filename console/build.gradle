eclipse.project.name = rootProject.name + '-core'

ext{
    artifactId = "action-system"
}

group += "console"
version = "$version"

dependencies{
    api "com.badlogicgames.gdx:gdx:$gdxVersion"
	implementation "space.earlygrey:shapedrawer:2.1.0"
}