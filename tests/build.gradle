eclipse.project.name = rootProject.name + '-tests'

dependencies {
	implementation project(":console")

	testImplementation 'junit:junit:4.+'
	testImplementation "org.mockito:mockito-all:1.9.+"
}

sourceSets {
	test {
        java {
            srcDir 'src'
        }
    }
}
